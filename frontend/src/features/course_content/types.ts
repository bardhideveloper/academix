/*export type LessonContent = {
  video_url?: string;
  article_text?: string;
  pdf_file?: string; 
};

export type Lesson = {
  id: number;
  title: string;
  content_type: "video" | "article" | "pdf";
  content: LessonContent;
  order: number;
  section_id: number;
  created_at: string;
  is_preview: boolean;
  can_access?: boolean;
};*/